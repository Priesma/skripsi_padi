{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "buildCommand": "pip install -r backend/requirements.txt"
  },
  "deploy": {
    "startCommand": "cd backend && MODEL_PATH=../best.pt uvicorn main:app --host 0.0.0.0 --port $PORT"
  }
}
